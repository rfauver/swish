# Swish

Fast NBA scores PWA. Built with React, TypeScript, Vite, and TanStack Query.

## Development

```bash
npm install
npm run dev
```

## Build & preview

```bash
npm run build
npx vite preview
```

Test offline behavior with the production build (`vite preview`), not the dev server.
